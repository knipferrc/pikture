import { Button } from "std-widgets.slint";

export component Sidebar {   
    VerticalLayout { 
        width: 100px;
        spacing: 10px;

        Button {
            height: 30px;
            text: "Home";
        }
        Button {
            height: 30px;
            text: "Downloads";
        }
        Button {
            height: 30px;
            text: "Desktop";
        }
     }
}