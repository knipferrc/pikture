import { Button, VerticalBox } from "std-widgets.slint";

export component FolderView inherits VerticalBox {   
    VerticalBox { 
        width: 100%;
        padding: 0px;

        Button {
            height: 100px;
            width: 100px;
            text: "Home";
        }
     }
}