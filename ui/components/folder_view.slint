import { Button } from "std-widgets.slint";

export component FolderView {   
    VerticalLayout { 
        width: 100%;
        padding: 0px;

        Button {
            height: 100px;
            width: 100px;
            text: "Home";
        }
     }
}