import { Sidebar } from "components/sidebar.slint";
import { FolderView } from "components/folder_view.slint";

export component AppWindow inherits Window {
    preferred-height: 400px;
    preferred-width: 700px;
    min-height: 300px;

    HorizontalLayout {  
        padding: 10px;
        alignment: start;

        Sidebar{}
        FolderView {}
     }
}